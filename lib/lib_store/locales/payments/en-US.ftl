# The tiers section of the Store: what the cloud backup uses, as the server
# reports it.
payments-usage-stored = { $used } of { $cap } kept in the cloud
payments-usage-transferred = { $used } of { $cap } moved this month
